<form>
  <% if defined?(alert_type) %>
    <div class="alert alert-<%= alert_type %>"><%= message %></div>
  <% end %>

  <div class="row">
    <div class="col col-6">
      <div class="form-group in-tabs">
        <label for="cpasswd">Contraseña actual</label>
        <input class="input-block" type="password" id="cpasswd" name="cpasswd"/>
      </div>
    </div>

    <div class="col col-6">
      <div class="form-group in-tabs">
        <label for="npasswd">Nueva contraseña</label>
        <input class="input-block" type="password" id="npasswd" name="npasswd"/>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col col-12 in-tabs btn-grp">
      <input class="paper-btn btn-block btn-secondary-outline" type="submit" value="Cambiar contraseña!" hx-post="/update-password" hx-target="closest form">
    </div>
  </div>
</form>
